<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MonteBravoTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/secondary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green_tag_color</item>
        <item name="colorSecondaryVariant">@color/gray_tag_color</item>
        <item name="colorOnSecondary">@color/primary_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MonteBravoTest.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MonteBravoTest.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="Theme.MonteBravoTest.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />
</resources>